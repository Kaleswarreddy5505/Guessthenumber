# Guessthenumber
A simple interactive game where the user guesses a randomly generated number with hints like “too high” or “too low” until the correct number is found.
The system provides hints after each attempt, guiding the user to guess correctly in fewer tries.

This project demonstrates basic programming concepts such as user input handling, conditional statements, loops, and random number generation.
